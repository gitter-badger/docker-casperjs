machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t vdmtl/docker-casperjs .

test:
  override:
  - docker run vdmtl/docker-casperjs casperjs --version
